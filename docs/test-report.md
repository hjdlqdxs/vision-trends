# 测试与验证报告

## 后续导航修复验证

用户实际体验反馈：会议筛选后点击热门方向进入论文库，缺少直接返回入口。修复增加带来源的返回链接，保留原会议/年份，覆盖总览、图谱、年度榜单、列表搜索及清词场景。6项导航回归测试与下述原16项测试合计22项全部通过，19个JavaScript文件语法检查通过。用户随后确认本次返回导航修复“符合我的预期”；仅记录为这一问题的人工验收，不等同于浏览器全流程自动化验收。记录见 `navigation-review.md`。

## 自动化功能测试

环境：Windows、Node.js v22.19.0，内置 SQLite；`npm.cmd test` / `npm test` 使用 `--experimental-test-isolation=none`，避免当前沙箱无法启动测试子进程。单个测试文件内每项使用独立内存库或临时文件。

16项测试全部通过，覆盖：短语提取与边界、会议年份与链接校验、CRUD/去重/查询/人工编辑保留、不同届次同名、SQLite重开持久化、文档频数/共现、缺失年份与真实零、CSV引号与公式保护、CVF/ECVA解析、摘要缺失、抓取域名白名单、反机器人响应识别、HTTP CRUD、口令和来源检查、在线回退/批量部分失败、清空标签重新提取。

命令：

```sh
npm test
npm run check
```

修复前：`test-before-fix.txt`，15通过/1失败。修复后：`test-after-fix.txt`，16通过/0失败。测试耗时为自动化运行时间，不是学生PSP工时。

## 真实联网数据采集

2026-09-20 对8个2022—2025会议届次各选择24条官方目录记录，实际获得189篇，3次网络失败。189篇均解析出官方摘要，来源和失败清单在 `data/provenance.json`。样本中会议分布 CVPR 94、ICCV 47、ECCV 48。

选择方法为官方目录顺序等间距，不是随机抽样。官网列表数量是采集时解析条目数，可能与会议最终官方录取总数不同；没有强行改成作业举例数字。

另运行 `node scripts/live-smoke.mjs`：通过真实HTTP接口抓取ICCV 2023《Segment Anything》，确认官方摘要、关键词、PDF及入库后精确查询均成功，同时确认首页和所有前端资源返回200。结果在 `docs/live-smoke.json`；使用临时内存库，没有污染演示库。这是网络/API验收，不替代浏览器渲染验收。

## 可视化材料

`scripts/export-analytics.mjs` 直接调用应用统计模块生成数据，Python matplotlib/Pillow 导出11张PNG、2张GIF。已查看Top10和最终趋势图检查文字、配色和数据标签。图表不是浏览器界面截图。

## 未完成的验证与限制

- 管理沙箱中 Chrome/Edge/headless-shell 因 Windows Mojo IPC 权限失败，浏览器全流程截图与真实渲染验收未完成。`scripts/capture-browser.*` 提供本机执行入口，待用户在普通会话验证。
- Figma已有8项模拟测试、用户执行154位置成功截图和返回总览可用确认；分享链接已由用户无痕验证。其余真实交互尚未全部验收。
- Docker在Render提交a9671ee构建并上线成功，用户提供日志截图。4项公网只读API检查通过，见cloud-readonly-check.json；该API检查不证明写入。随后用户另行确认云端CRUD通过，并提供单篇采集等5张截图；随后TXT批量导入有2/2成功截图；热度走势播放、暂停、拖动、切换指标也已由用户确认正常，并提供3张状态截图；图谱节点筛选与返回也已由用户确认正常并提供2张截图；CSV、部分失败与年度演变等其他路径仍待验收。Compose未实际部署。
- 外部站点可能变更结构，DBLP 本次触发反机器人验证；应用会明确报告，无结果不合成假论文。

## 待用户验收表

| 检查 | 预期 | 实际 |
|---|---|---|
| 云端URL | 外部浏览器可访问 | Render无痕走势页截图及4项只读API检查通过 |
| 云端CRUD | 新增、编辑、删除成功 | 用户文字确认成功；过程截图待补 |
| 单篇采集与查询 | 摘要/标签/链接/精确查询正常 | 用户5张截图并确认正常，Segment Anything，ICCV2023 |
| 文件导入 | CSV/TXT识别并逐条返回 | TXT：用户截图显示识别2个标题，ICCV2023成功2/2；第一条已存在、未覆盖。CSV及部分失败待验收 |
| 图谱联动 | 点击后关键词筛选正确 | 用户截图显示点击节点进入 `3D vision` 筛选结果，且可返回研究总览 |
| 趋势动画 | 播放/暂停/滑块一致，指标可切换 | 用户确认四项操作均正常；3张截图展示3D vision的2025/2023占比和2025数量状态 |
| 手机布局 | 390px无整体横向溢出 | 待填写 |
| 重启持久化 | 编辑后重启仍保留 | Render Free无持久磁盘，不保证保留；本地SQLite持久化测试通过 |
| 原型链接 | 无需作者账号即可访问 | 用户确认无痕访问成功 |
